- バージョン：v2
- 作成日：2026-09-05
- 開発方式：ウォーターフォール
- ステータス：確定版（v1の内容を引き継ぎ、画面ID・自動計算ロジック・ルート提案仕様などを反映して新規作成）

---

## 機能一覧

確定済みの機能のみ（拡張候補は含まない）。機能名・画面IDから、本文の画面詳細・仕様へジャンプできる。

参照：[スマホアプリ側 要件定義書 v1](#nav:要件定義書:スマホアプリ:v1)

### 初期設定系（初回起動時のみ）

| 機能 | 概要 | 主な画面ID |
|---|---|---|
| [オンボーディング](#s1-オンボーディング) | アプリの概要を紹介する | [S1](#s1-オンボーディング) |
| [身体情報入力](#s1-身体情報入力) | 身長・体重・性別を初期入力する | [S1'](#s1-身体情報入力) |
| [位置情報許可の訴求](#s2-位置情報許可の訴求) | 走行計測に必要な位置情報の許可を、地図を用いて訴求する | [S2](#s2-位置情報許可の訴求) |

※ 上記3機能は初回起動時のみ表示。2回目以降はスキップし、ホームから開始する。

### ホーム・接続系

| 機能 | 概要 | 主な画面ID |
|---|---|---|
| [ホーム画面（前回記録表示）](#s3-ホーム前回記録表示) | 地図を背景に、前回の走行記録をカードで表示する。データがない場合は「-」表示。カードタップで履歴へ遷移 | [S3](#s3-ホーム前回記録表示) |
| [Readyチェック機能（デバイス接続）](#s4-デバイス接続) | ARグラス（必須）・Apple Watch・AirPods（任意）の接続状況を管理する。必須／任意は視覚的に区別する | [S4](#s4-デバイス接続) |

### ランニング設定・ルート系

| 機能 | 概要 | 主な画面ID |
|---|---|---|
| [ランニング設定（ハイブリッド入力）](#s5-ランニング設定) | ドラムロール・+/-ボタン・直接入力の3方式で、時間・距離・ペースを設定する | [S5](#s5-ランニング設定) |
| [自動計算ロジック](#41-s5-ランニング設定：自動計算ロジック) | 時間・距離・ペースは相互依存。距離は自動修正の対象とせず、編集していない方の値（主にペースまたは時間）を自動更新する。無効な値になる場合はポップアップ通知後、直前の有効値に戻す | [S5](#41-s5-ランニング設定：自動計算ロジック) |
| [ルート提案](#42-s8-ルート提案：詳細仕様) | おすすめルート・過去のルートを、目標距離に近い順に複数提示する。候補がなければ終了地点選択で自動算出（カーナビ的操作）。候補選択時はポップアップでルート全体像・距離・推定時間・ペースを表示 | [S8](#42-s8-ルート提案：詳細仕様) |
| [ルートカスタマイズ](#s8-1-ルートカスタマイズ) | ユーザーが地図上でルートを自分で詳細に指定する | [S8-1](#s8-1-ルートカスタマイズ) |

### 走行中・記録系

| 機能 | 概要 | 主な画面ID |
|---|---|---|
| [誤操作防止・スリープ制御](#s6-ランニング中ロック画面) | 走行開始後、自動スリープを無効化しつつ、誤操作を防ぐガードレイヤーを表示する | [S6](#s6-ランニング中ロック画面) |
| [リザルト・スコアリング](#s7-ラン結果) | シンクロ率（%）を4段階ランクに変換し、ランクごとにバッジ・コメントを表示する | [S7](#s7-ラン結果) |
| [身体負荷・疲労推定](#s7-ラン結果) | 気温を係数とした疲労度を算出し、結果画面に表示する | [S7](#s7-ラン結果) |
| [走行終了後の画面遷移](#s7-ラン結果) | 「終了」でホーム画面に戻る | [S7→S3](#s7-ラン結果) |

### データ管理・履歴系

| 機能 | 概要 | 主な画面ID |
|---|---|---|
| [セーフティ・ロギング](#43-セーフティ-ロギング) | 急停止・速度超過・逸脱地点をルートマップ上にログ記録する | - |
| [走行履歴の保存・閲覧](#s10-ランニング履歴) | 走行データをアプリ内に保存し、過去の履歴を一覧で確認できる（外部サービス連携なし） | [S10](#s10-ランニング履歴) |

### 設定・権限管理系

| 機能 | 概要 | 主な画面ID |
|---|---|---|
| [設定メニュー](#s9-設定メニュー) | オンボーディング見返し・身体情報再設定・ARグラス表示設定・履歴確認への入口 | [S9](#s9-設定メニュー) |
| [オンボーディング見返し](#s9-1-オンボーディング見返し) | S1のオンボーディングを再度閲覧する | [S9-1](#s9-1-オンボーディング見返し) |
| [身体情報の再設定](#s9-2-身体情報の再設定) | S1'で入力した情報を再設定する | [S9-2](#s9-2-身体情報の再設定) |
| [ARグラス表示設定](#s9-3-arグラス表示設定) | HUD表示項目のON/OFF、走行分析結果の表示可否を設定する | [S9-3](#s9-3-arグラス表示設定) |
| [権限管理・フォールバック](#44-権限管理-フォールバック) | カメラ権限拒否時はHUDモードのみで継続。GPS/BT拒否時は出走不可とし設定画面へ誘導 | [S4](#44-権限管理-フォールバック) |

---

## 1. 概要

本文書は、スマホアプリ側 要件定義書で定義した機能を、どのような画面構成・画面遷移で実現するかを示すものである。

実装済み画面のスクリーンショットレビュー、および実装側で共有されたフロー図との照合をもとに作成しており、画面IDは実装側の呼称（S1〜S10）に合わせている。

※ 画面IDの数字は実装順を厳密に表すものではない（S8のルート提案がS6・S7より先に登場するなど）。実装側の呼称をそのまま踏襲しているため、番号と画面順序が一致しない箇所がある点に注意。

---

## 2. 画面構成（画面一覧）

画面遷移順に記載する。先頭の[機能一覧](#機能一覧)から各画面へジャンプできる。実装側で共有された画面モック（`assets/screens`（元データ: `assets/画面構成`））がある画面は、**画像 → 使用詳細**の順で1画面ずつ記載する。

### S1 オンボーディング

※ 本フォルダに該当画像なし（スクリーンショット未確認）。

- **画面名**：オンボーディング（3枚組）
- **概要**：WELCOME → AVATAR → SYNC の順で、アプリのコンセプトを紹介する導入画面（初回起動時のみ）
- **使用詳細**：
  - 初回起動時のみ表示し、完了後は S1'（身体情報入力）へ進む
  - 2回目以降の起動ではスキップする
  - 設定メニューから見返し可能（S9-1）
- **対応する要件定義書の項目**：4.2 オンボーディング

### S1' 身体情報入力

※ 本フォルダに該当画像なし（実装フロー図で画面存在は確認済み。中身のスクリーンショットは未確認）。

- **画面名**：身体情報入力
- **概要**：身長・体重・性別を初期入力する画面（初回起動時のみ）
- **使用詳細**：
  - 初回起動フローの中で、オンボーディング完了後に表示する
  - 入力値は身体負荷・疲労推定などに用いる
  - 後から設定メニュー（S9-2）で再設定できる
- **対応する要件定義書の項目**：4.2 身体情報入力

### S2 位置情報許可の訴求

※ 本フォルダに該当画像なし（スクリーンショット未確認）。

- **画面名**：位置情報許可の訴求
- **概要**：地図を背景に、位置情報許可の必要性を訴求する画面（初回起動時のみ）
- **使用詳細**：
  - 走行計測に必要な位置情報許可を、地図UIを用いて分かりやすく訴求する
  - 許可後（またはフロー完了後）にホーム（S3）へ遷移する
  - 2回目以降の起動ではスキップする
- **対応する要件定義書の項目**：4.2 位置情報許可の訴求

### S3 ホーム（前回記録表示）

![ホーム画面（S3）](assets/screens/s3-home.png)

- **画面名**：ホーム（前回記録表示）
- **概要**：地図を背景に、上部に前回記録カード（初回は「-」表示）を表示するメイン画面。2回目以降の起動時は、アプリ起動後ここに直接遷移する
- **使用詳細**：
  - ヘッダーに「前回の記録」とハンバーガーメニュー（設定入口）を配置する
  - 上部カードに、前回ルートの地図プレビューと、日付・距離・速さ・時間を表示する
  - データがない場合は各項目を「-」で示す
  - 前回記録カードをタップすると、ランニング履歴（S10）へ遷移する
  - 下部カードに案内テキストと、スワイプ操作によるラン開始導線を置く
  - ラン開始操作後はデバイス接続（S4）へ進む
- **対応する要件定義書の項目**：4.2 ホーム画面（前回記録表示）

### S4 デバイス接続

![接続画面（S4）](assets/screens/s4-connect.png)

- **画面名**：デバイス接続
- **概要**：ARグラス・Apple Watch・AirPodsの接続状況を管理する画面。ホームからラン開始操作をするたびに表示する
- **使用詳細**：
  - タイトル「デバイスを接続」、説明「使用するデバイスを接続してください」
  - デバイス行を一覧表示し、各行に接続操作（「接続」）を置く
  - 必須（ARグラス）／任意（Apple Watch・AirPods）は視覚的に区別する
  - 「戻る」でホーム（S3）側へ戻れる
  - 下部の進むボタンでランニング設定（S5）へ進む
- **関連仕様**：[権限管理・フォールバック](#44-権限管理-フォールバック)
- **対応する要件定義書の項目**：4.2 Readyチェック機能（デバイス接続）

### S5 ランニング設定

![ランニング設定画面（S5）](assets/screens/s5-run-settings.png)

- **画面名**：ランニング設定
- **概要**：時間・距離・ペースの3値を設定する画面。ドラムロール・+/-ボタン・直接入力の3方式で編集可能
- **使用詳細**：
  - タイトル「ペースを設定」、説明「目標を設定してください」
  - 時間・距離・ペースをカード形式で並べ、直接入力欄と微調整（+/-）を提供する
  - 3値は相互依存。距離は基本的に自動修正の対象としない
  - 「戻る」で前画面へ戻る。進む操作でルート提案（S8）へ遷移する
  - 詳細な自動更新ルールは [4.1 自動計算ロジック](#41-s5-ランニング設定：自動計算ロジック) を参照
- **対応する要件定義書の項目**：4.2 ランニング設定（ハイブリッド入力）
- **詳細仕様**：[4.1 自動計算ロジック](#41-s5-ランニング設定：自動計算ロジック)

### S8 ルート提案

![ルート選択画面（S8）](assets/screens/s8-route.png)

- **画面名**：ルート提案
- **概要**：おすすめルート・過去に走ったルートを候補として複数提示する画面（S5の目標距離に近い順）
- **使用詳細**：
  - タイトル「ルートを選択」、説明「ルートを選択してください」
  - 上部に選択中ルートの全体マップ（スタート／ゴール／経路）を表示する
  - 下部にルート候補カード（ルート名・サムネ地図・距離）を横並びで提示する
  - 候補がない／好みに合わない場合は、地図上で終了地点を選んでルート自動算出できる（カーナビ的操作）
  - 候補選択時はポップアップでルート全体像・距離・推定時間・ペースを表示する
  - 「ランニングを開始」で走行中ロック（S6）へ進む。カスタマイズは S8-1 へ
  - 詳細は [4.2 ルート提案の詳細](#42-s8-ルート提案：詳細仕様) を参照
- **対応する要件定義書の項目**：4.2 ルート提案
- **詳細仕様**：[4.2 ルート提案の詳細](#42-s8-ルート提案：詳細仕様)

### S8-1 ルートカスタマイズ

※ 本フォルダに該当画像なし。

- **画面名**：ルートカスタマイズ
- **概要**：ユーザーが地図上でルートを自分で詳細に指定する画面
- **使用詳細**：
  - S8の「カスタマイズ」から遷移する
  - 地図上で経路を詳細指定し、指定後はルート提案フロー（走行開始）へ戻る
- **対応する要件定義書の項目**：4.2 ルート提案
- **関連仕様**：[4.2 ルート提案の詳細](#42-s8-ルート提案：詳細仕様)

### S6 ランニング中（ロック画面）

![走行中ロック画面（S6）](assets/screens/s6-lock.png)

- **画面名**：ランニング中（ロック画面）
- **概要**：走行中の誤操作を防ぐガードレイヤー画面。自動スリープを無効化しつつ、誤操作を防ぐ
- **使用詳細**：
  - 走行開始後に表示する。画面中央にロックアイコンを置き、誤タップを防ぐ
  - 走行中は自動スリープを無効化する
  - 画面操作の主体はARグラス側HUDを想定し、スマホはガード状態を維持する
  - 走行終了操作後にラン結果（S7）へ遷移する
- **対応する要件定義書の項目**：4.2 誤操作防止・スリープ制御

### S7 ラン結果

![結果画面（S7）](assets/screens/s7-result.png)

- **画面名**：ラン結果
- **概要**：走行後の記録を表示する画面。ランク（シンクロ率%）・バッジ・コメント、距離、タイム、平均ペース、疲労度などを表示する
- **使用詳細**：
  - タイトル「ランニング結果」
  - 上部に目標達成率（シンクロ率を基にした達成表示）を円形インジケータで示す
  - 中央に距離・タイム・平均ペース・詳細への入口を配置する
  - 「詳細」側に、メイン表示に含めない項目（消費カロリー等）や疲労度を格納する
  - 「履歴を見る」でランニング履歴（S10）へ、「終了」でホーム（S3）へ戻る
- **対応する要件定義書の項目**：4.1 リザルト・スコアリング／身体負荷・疲労推定／4.2 走行終了後の画面遷移

### S9 設定メニュー

![ホーム画面ハンバーガーメニュー（S9入口）](assets/screens/s9-menu.png)

- **画面名**：設定メニュー
- **概要**：ホーム画面（S3）のハンバーガーメニューからアクセスする設定一覧
- **使用詳細**：
  - ホーム右上のハンバーガーボタンを押すと、メニューが展開する
  - モック上の項目例：過去の履歴／AR設定／スマホ設定
  - 「過去の履歴」→ S10、「AR設定」→ S9-3、「スマホ設定」→ スマホ操作設定画面
  - 要件定義上の入口として、オンボーディング見返し（S9-1）・身体情報の再設定（S9-2）も設定系統から辿れる想定
- **対応する要件定義書の項目**：4.2 設定メニュー

### スマホ設定（設定メニュー内）

![スマホ操作設定画面](assets/screens/s9-phone-settings.png)

- **画面名**：スマホ設定
- **概要**：ハンバーガーメニューの「スマホ設定」から開く、スマホ側操作・表示に関する設定画面
- **使用詳細**：
  - タイトル「スマホ設定」。上部に「戻る」
  - ON/OFFトグルと、選択式の項目（小見出し追加など）を一覧で扱う
  - 設定変更後は「戻る」で前画面（ホームまたは設定入口）へ戻る
- **補足**：専用の画面IDは未採番。設定メニュー（S9）配下の画面として扱う

### S9-1 オンボーディング見返し

※ 本フォルダに該当画像なし。

- **画面名**：オンボーディング見返し
- **概要**：S1のオンボーディングを再度閲覧できる画面
- **使用詳細**：設定メニューからアクセスし、初回導入コンテンツを再表示する
- **対応する要件定義書の項目**：4.2 設定メニュー

### S9-2 身体情報の再設定

※ 本フォルダに該当画像なし。

- **画面名**：身体情報の再設定
- **概要**：S1'で入力した身長・体重・性別を再設定する画面
- **使用詳細**：設定メニューからアクセスし、身体情報を更新する
- **対応する要件定義書の項目**：4.2 設定メニュー

### S9-3 ARグラス表示設定

![AR設定画面（S9-3）](assets/screens/s9-3-ar-settings.png)

- **画面名**：ARグラス表示設定
- **概要**：HUD表示項目（ペース・心拍数など）のON/OFF、走行分析結果の表示可否を設定する画面
- **使用詳細**：
  - タイトル「AR設定」。上部に「戻る」
  - HUD／分析表示の各項目をトグル（ON/OFF）で切り替える
  - 変更内容はARグラス側の表示に反映する
- **対応する要件定義書の項目**：4.2 設定メニュー

### S10 ランニング履歴

![ランニング履歴一覧画面（S10）](assets/screens/s10-history.png)

- **画面名**：ランニング履歴
- **概要**：過去の走行履歴を一覧で表示する画面。走行データはアプリ内に保存し、外部サービス連携は行わない
- **使用詳細**：
  - タイトル「ランニング履歴」、説明「詳細を確認する場合はタップ」
  - 各行に地図サムネ・日付・時間・シンクロ率・距離を表示する
  - ホームの前回記録カード、結果画面の「履歴を見る」、設定メニューの「過去の履歴」から到達する
  - 「戻る」で元の画面へ戻る

![ランニング履歴詳細（タップ時）](assets/screens/s10-history-detail.png)

- **使用詳細（履歴タップ時）**：
  - 一覧のカードをタップすると、詳細ポップアップを前面に表示する
  - 詳細にはルート地図、日付、距離、時間、速さ、心拍数、達成率、天候、気温などを含む
  - 背景の一覧は暗転し、詳細確認に集中できる表示とする
- **対応する要件定義書の項目**：4.1 走行履歴の保存・閲覧

---

## 3. 画面遷移フロー（概要）

### 3.0 画面遷移図（実装側共有）

<img src="assets/画面遷移図.png" alt="画面遷移図" style="max-width:72%; height:auto; margin:16px auto; display:block;">

実装側で共有された画面遷移図。画面ID（S1〜S10）および遷移順の照合根拠とする。

### 3.1 起動時の分岐

```
アプリ起動
  ├─ 初回起動 ──────→ S1 オンボーディング → S1' 身体情報入力 → S2 位置情報許可 → S3 ホーム
  └─ 2回目以降の起動 ─→ S3 ホーム（S1・S1'・S2をすべてスキップ）
```

### 3.2 メイン画面遷移

```
S3 ホーム（前回記録表示）
  ├─ 前回記録カードをタップ → S10 ランニング履歴
  ├─ 「☰」（ハンバーガーメニュー）→ S9 設定メニュー
  │       ├─ オンボーディング見返し → S9-1
  │       ├─ 身体情報の再設定 → S9-2
  │       ├─ ARグラス表示設定 → S9-3
  │       └─ 履歴の確認 → S10 ランニング履歴
  │     （各サブ画面から S3 ホームへ戻る）
  └─ ラン開始操作
       ↓
     S4 デバイス接続（ARグラス／Apple Watch／AirPods）
       ↓
     S5 ランニング設定（時間／距離／ペース）
       ↓
     S8 ルート提案（候補提示／終了地点選択で自動算出）
       ├─ 「カスタマイズ」→ S8-1 ルートカスタマイズ
       └─ 「ランを開始」
            ↓
          S6 ランニング中（ロック画面）
            └─ 走行終了操作
                 ↓
               S7 ラン結果（シンクロ率・記録表示）
                 ├─ 「履歴を見る」→ S10 ランニング履歴
                 └─ 「終了」→ S3 ホームへ戻る
```

※ S4・S5・S8（デバイス接続・ランニング設定・ルート提案）は、ホームからラン開始操作をするたびに毎回表示する。
※ S9入口（ハンバーガー）、スマホ設定、S9-3（AR設定）のモックは `assets/screens`（元データ: `assets/画面構成`） に収録。S9-1・S9-2は未収録のため実装状況の確認が必要。

---

## 4. 画面別 詳細仕様

### 4.1 S5 ランニング設定：自動計算ロジック

時間・距離・ペースの3値は相互に依存する関係にあり（例：ペース＝時間÷距離）、3値とも自由に編集できる。編集時の自動更新ルールは以下の通り。

画面概要は [S5 ランニング設定](#s5-ランニング設定) を参照。

**自動更新の優先順位**

距離は誤入力が考えにくいため、基本的に自動修正の対象としない。

| ユーザーが直近に編集した項目 | 自動更新される項目 |
|---|---|
| 距離のみ | ペース |
| 時間のみ | ペース |
| ペースのみ | 時間 |
| ペース → 時間（両方を続けて編集） | 距離 |

**更新タイミング**

- **通常時（自動更新の結果が有効な値の場合）**：ポップアップなしで即座に反映する
- **無効な値になる場合**（0除算・非現実的な数値など）：ポップアップで通知したうえで、直前の有効な値に自動的に戻す

---

### 4.2 S8 ルート提案：詳細仕様

画面概要は [S8 ルート提案](#s8-ルート提案) ／ [S8-1 ルートカスタマイズ](#s8-1-ルートカスタマイズ) を参照。

**候補の提示**

- おすすめルート・過去に走ったルートを候補として複数提示する
- 候補は、S5で設定した目標距離に近い順に並べる

**候補がない場合の代替手段**

- 好みの候補がない場合、地図上で終了地点を選択すると、ルートを自動算出する（カーナビのような操作感）

**さらに細かく指定したい場合**

- 「カスタマイズ」ボタンから、S8-1（ルートカスタマイズ画面）へ遷移し、ユーザー自身が地図上でルートを詳細に指定できる

**候補選択時のポップアップ**

- 候補（または終了地点選択で自動算出されたルート）を選択すると、ポップアップでルート全体像（地図）・距離・推定時間・ペースを表示する

---

### 4.3 セーフティ・ロギング

急停止・速度超過・逸脱地点をルートマップ上にログとして記録する（画面IDなし。走行分析・データマネジメント要件）。

---

### 4.4 権限管理・フォールバック

[S4 デバイス接続](#s4-デバイス接続) および出走前の権限確認に関連する。

- **カメラ権限拒否時**：アバター表示を停止し、HUD（数値）モードのみで走行計測を継続する
- **必須権限（GPS/BT）拒否時**：出走不可とし、OS設定画面への誘導を表示する

---

## 5. 未確認・要確認事項

- **S1 / S1' / S2**：初回起動系の画面モックは `assets/screens`（元データ: `assets/画面構成`） に未収録。S1'は実装フロー図で存在確認済みだが、入力UIの中身は未確認
- **S8-1 ルートカスタマイズ**：画面モック未収録。実装状況を確認する
- **権限拒否時のフォールバック画面**（カメラ権限拒否時のHUDモード表示、GPS/BT拒否時のOS設定誘導）：スクリーンショット未確認。実装状況を確認する
- **S9-1 / S9-2**：オンボーディング見返し・身体情報再設定の画面モックは未収録。ハンバーガーメニュー項目（過去の履歴／AR設定／スマホ設定）との対応も含め、実装との整合を確認する
- **S3 前回記録カードの空状態表示**：データがない場合の「-」表示について、スクリーンショットでは未確認。実装状況を確認する

---

## 6. 改訂履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| v2 | 2026-09-05 | v1の内容（画面ID・画面遷移・S5自動計算ロジック・S8ルート提案仕様など）を引き継ぎ、v2として新規作成。文書先頭に機能一覧を組み込み、各画面見出しへのリンクを追加。v1までの詳細な更新履歴は スマホアプリ側_基本設計書_v1.md を参照 |
