- バージョン：v1
- 作成日：2026-09-04
- 開発方式：ウォーターフォール
- ステータス：初版（実装済み画面のレビュー＋実装フロー図の照合をもとに作成）

---

## 1. 概要

本文書は、スマホアプリ側 要件定義書で定義した機能を、どのような画面構成・画面遷移で実現するかを示すものである。

実装済み画面のスクリーンショットレビュー、および実装側で共有された画面遷移図との照合をもとに作成しており、画面IDは画面遷移図の呼称（S1〜S10）に合わせている。要件定義書との差分があった箇所は「4. 要修正事項」として明記している。基本設計レビューでの確認・修正依頼に用いる。

---

## 2. 画面構成（画面一覧）

画面遷移順に記載する。

| 画面ID | 画面名 | 概要 | 対応する要件定義書の項目 |
|---|---|---|---|
| S1 | オンボーディング（3枚組） | WELCOME → AVATAR → SYNC の順で、アプリのコンセプトを紹介する導入画面（初回起動時のみ） | 4.2 オンボーディング |
| S1' | 身体情報入力 | 身長・体重・性別を初期入力する画面（初回起動時のみ）。実装フロー図で画面自体の存在は確認できたが、中身のスクリーンショットは未確認 | 4.2 身体情報入力 |
| S2 | 位置情報許可の訴求 | 地図を背景に、位置情報許可の必要性を訴求する画面（初回起動時のみ） | 4.2 位置情報許可の訴求 |
| S3 | ホーム（前回記録表示） | 地図を背景に、上部に前回記録カード（初回は「-」表示）を表示するメイン画面。2回目以降の起動時は、アプリ起動後ここに直接遷移する | 4.2 ホーム画面（前回記録表示） |
| S4 | デバイス接続 | ARグラス・Apple Watch・AirPodsの接続状況を管理する画面。ホームからラン開始操作をするたびに表示する | 4.2 Readyチェック機能（デバイス接続） |
| S5 | ランニング設定 | 時間・距離・ペースの目標値を設定する画面 | 4.2 ランニング設定（ハイブリッド入力） |
| S6 | ルート設定 | 現在地周辺のおすすめルートを地図上に表示し、ランを開始する画面（ランニング設定の後、ランニング中ロックの前に表示） | 4.2 ルート提案 |
| S7 | ランニング中（ロック画面） | 走行中の誤操作を防ぐガードレイヤー画面 | 4.2 誤操作防止・スリープ制御 |
| S8 | ランニング結果 | 走行後のシンクロ率・距離・タイム・平均ペース・消費カロリーなどを表示する画面。「終了」でホーム（S3）に戻る | 4.1 リザルト・スコアリング／4.2 走行終了後の画面遷移 |
| S9 | 設定メニュー | ホーム画面（S3）のハンバーガーメニューからアクセスする設定一覧画面 | 4.2 設定メニュー |
| S9-1 | オンボーディング見返し | S1のオンボーディングを再度閲覧できる画面 | 4.2 設定メニュー |
| S9-2 | 身体情報の再設定 | S1'で入力した身長・体重・性別を再設定する画面 | 4.2 設定メニュー |
| S9-3 | ARグラス表示設定 | HUD表示項目（ペース・心拍数など）のON/OFF、走行分析結果の表示可否を設定する画面 | 4.2 設定メニュー |
| S10 | 履歴一覧 | 過去の走行履歴を一覧で表示する画面 | 4.1 走行履歴の保存・閲覧 |

---

## 3. 画面遷移フロー（概要）

### 3.0 画面遷移図（実装側共有）

![画面遷移図](assets/画面遷移図.png)

実装側で共有された画面遷移図。画面ID（S1〜S10）および遷移順の照合根拠とする。

### 3.1 起動時の分岐

```
アプリ起動
  ├─ 初回起動 ──────→ S1 オンボーディング → S1' 身体情報入力 → S2 位置情報許可 → S3 ホーム
  └─ 2回目以降の起動 ─→ S3 ホーム（S1・S1'・S2をすべてスキップ）
```

### 3.2 メイン画面遷移

```
S3 ホーム（前回記録表示）
  ├─ 前回記録カードをタップ → S10 履歴一覧
  ├─ 「☰」（ハンバーガーメニュー）→ S9 設定メニュー
  │       ├─ オンボーディング見返し → S9-1
  │       ├─ 身体情報の再設定 → S9-2
  │       ├─ ARグラス表示設定 → S9-3
  │       └─ 履歴の確認 → S10 履歴一覧
  │     （各サブ画面から S3 ホームへ戻る）
  └─ ラン開始操作
       ↓
     S4 デバイス接続（ARグラス／Apple Watch／AirPods）
       ↓
     S5 ランニング設定（時間／距離／ペース）
       ↓
     S6 ルート設定（おすすめルート表示）
       └─ 「ランを開始」
            ↓
          S7 ランニング中（ロック画面）
            └─ 走行終了操作
                 ↓
               S8 ランニング結果（シンクロ率・記録表示）
                 ├─ 「履歴を見る」→ S10 履歴一覧
                 └─ 「終了」→ S3 ホームへ戻る
```

※ S4・S5・S6（デバイス接続・ランニング設定・ルート設定）は、ホームからラン開始操作をするたびに毎回表示する。
※ S9系統の画面（設定メニュー、S9-1〜S9-3）は今回のスクリーンショットには含まれていない。実装状況の確認が必要。

---

## 4. 要修正事項（レビュー指摘）

要件定義書と実装済み画面を照らし合わせ、以下の差分を確認した。基本設計レビューまでに方針を確認し、必要に応じて実装への修正依頼を行う。

| No | 画面 | 内容 | 対応方針 |
|---|---|---|---|
| 1 | S5 ランニング設定 | 要件定義書では「ドラムロール・+/-ボタン・直接入力」の3方式ハイブリッド入力としているが、現状の画面は+/-ボタンのみに見える | **要修正**：ドラムロール（スワイプ操作）・直接入力（タップして数値入力）を追加実装する |
| 2 | S4 デバイス接続 | ARグラス（必須）とApple Watch・AirPods（任意）が、画面上は同列の「接続」ボタンで区別がつかない | **要修正**：必須／任意が視覚的に区別できるようにする（ラベル表示・色分け等） |

---

## 5. 未確認・要確認事項

- **S1' 身体情報入力画面**：実装フロー図で画面の存在は確認できたが、画面の中身（入力項目のUI）は未確認。実装状況を確認する
- **権限拒否時のフォールバック画面**（カメラ権限拒否時のHUDモード表示、GPS/BT拒否時のOS設定誘導）：スクリーンショット未確認。実装状況を確認する
- **S8 ランニング結果画面の表示項目**：「消費カロリー」表示、「TARGET ACHIEVED」バッジなど、要件定義書に明記していない項目がある。企画書にあった「4段階ランク（Perfect〜Try Again）」表現ではなく、シンクロ率（%）＋達成バッジという表現になっている。要件定義書側への追記要否を検討する
- **S9 設定メニュー系統（S9, S9-1, S9-2, S9-3）**：今回のスクリーンショットには含まれておらず、実装状況が未確認。特にS9-3（ARグラス表示設定）は、HUD表示項目のON/OFFという比較的幅広い設定内容を想定しているため、実装との整合を優先的に確認する
- **S3 前回記録カードの空状態表示**：データがない場合の「-」表示について、スクリーンショットでは未確認。実装状況を確認する

---

## 6. 改訂履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| v1 | 2026-09-04 | 初版作成。実装済み画面（オンボーディング〜履歴表示まで）をもとに画面構成・遷移・要修正事項を整理 |
| v1（更新） | 2026-09-04 | 設定メニュー系統（S9, S9-1〜S9-3）を画面構成・遷移フローに追加。実装状況は未確認のため要確認事項とした |
| v1（更新2） | 2026-09-04 | ホーム画面（前回記録表示）を追加。前回記録カードのタップ導線を追加 |
| v1（更新3） | 2026-09-04 | オンボーディング・身体情報入力は初回起動時のみ表示する仕様を反映。起動時の分岐（初回／2回目以降）を追記 |
| v1（更新4） | 2026-09-04 | 実装側フロー図との照合により、画面ID・順序を全面的に修正。画面IDを実装側呼称（S1〜S10）に統一。位置情報許可も初回のみに変更。デバイス接続・ランニング設定・ルート提案はホーム画面の後（ラン開始操作時）に毎回表示される順序に修正。走行終了後はホーム画面に戻る仕様を明記 |
| v1（更新5） | 2026-09-04 | 実装側共有の画面遷移図（assets/画面遷移図.png）を文書に追加 |
| v1（更新6） | 2026-09-04 | 画面遷移図に合わせて画面IDを再統一（S6=ルート設定、S7=ランニング中ロック、S8=ランニング結果、S10=履歴一覧） |
