- バージョン：v1
- 作成日：2026-09-04
- 開発方式：ウォーターフォール
- ステータス：初版（実装済み画面のレビューをもとに作成）

---

## 1. 概要

本文書は、スマホアプリ側 要件定義書で定義した機能を、どのような画面構成・画面遷移で実現するかを示すものである。

実装済み画面のスクリーンショットレビューをもとに作成しており、要件定義書との差分があった箇所は「4. 要修正事項」として明記している。基本設計レビューでの確認・修正依頼に用いる。

---

## 2. 画面構成（画面一覧）

| 画面ID | 画面名 | 概要 | 対応する要件定義書の項目 |
|---|---|---|---|
| S1 | オンボーディング（3枚組） | WELCOME → AVATAR → SYNC の順で、アプリのコンセプトを紹介する導入画面 | 4.2 オンボーディング |
| S1' | 身体情報入力 | 身長・体重・性別を初期入力する画面（※スクリーンショット未確認、実装状況要確認） | 4.2 オンボーディング |
| S2 | 位置情報許可の訴求 | 地図を背景に、位置情報許可の必要性を訴求する画面 | 4.2 位置情報許可の訴求 |
| S3 | デバイス接続 | ARグラス・Apple Watch・AirPodsの接続状況を管理する画面 | 4.2 Readyチェック機能 |
| S4 | ランニング設定 | 時間・距離・ペースの目標値を設定する画面 | 4.2 ランニング設定（ハイブリッド入力） |
| S5 | ホーム（前回記録表示・ルート提案） | 地図を背景に、上部に前回記録カード（初回は「-」表示）、下部におすすめルートとランを開始ボタンを表示する画面 | 4.2 ホーム画面（前回記録表示）／ルート提案 |
| S9 | 設定メニュー | マップ画面（S5）のハンバーガーメニューからアクセスする設定一覧画面 | 4.2 設定メニュー |
| S9-1 | オンボーディング見返し | S1のオンボーディングを再度閲覧できる画面 | 4.2 設定メニュー |
| S9-2 | 身体情報の再設定 | S1'で入力した身長・体重・性別を再設定する画面 | 4.2 設定メニュー |
| S9-3 | ARグラス表示設定 | HUD表示項目（ペース・心拍数など）のON/OFF、走行分析結果の表示可否を設定する画面 | 4.2 設定メニュー |
| S6 | ランニング中（ロック画面） | 走行中の誤操作を防ぐガードレイヤー画面 | 4.2 誤操作防止・スリープ制御 |
| S7 | ラン結果 | 走行後のシンクロ率・距離・タイム・平均ペース・消費カロリーなどを表示する画面 | 4.1 リザルト・スコアリング |
| S8 | ランニング履歴 | 過去の走行履歴を一覧で表示する画面 | 4.1 走行履歴の保存・閲覧 |

---

## 3. 画面遷移フロー（概要）

```
S1 オンボーディング（WELCOME→AVATAR→SYNC）
  └─ 「はじめる」
       ↓
S1' 身体情報入力（※実装状況要確認）
       ↓
S2 位置情報許可の訴求
  └─ 「Get started」
       ↓
S3 デバイス接続（ARグラス／Apple Watch／AirPods）
  └─ 「→」（未接続でもスキップ可）
       ↓
S4 ランニング設定（時間／距離／ペース）
  └─ 「→」
       ↓
S5 ホーム（前回記録表示・ルート提案）
  ├─ 前回記録カードをタップ → S8 ランニング履歴
  ├─ 「ランを開始」→ S6 ランニング中
  └─ 「☰」（ハンバーガーメニュー）
       ↓
     S9 設定メニュー
       ├─ オンボーディング見返し → S9-1
       ├─ 身体情報の再設定 → S9-2
       ├─ ARグラス表示設定 → S9-3
       └─ 履歴の確認 → S8 ランニング履歴

S6 ランニング中（ロック画面）
  └─ 走行終了操作
       ↓
S7 ラン結果（シンクロ率・記録表示）
  ├─ 「履歴を見る」→ S8 ランニング履歴
  └─ 「終了」→ ホームへ
```

※ S8（ランニング履歴）から前の画面（ホーム等）への戻り導線は、スクリーンショットからは未確認。
※ S9系統の画面（設定メニュー、S9-1〜S9-3）は今回のスクリーンショットには含まれていない。実装状況の確認が必要。

---

## 4. 要修正事項（レビュー指摘）

要件定義書と実装済み画面を照らし合わせ、以下の差分を確認した。基本設計レビューまでに方針を確認し、必要に応じて実装への修正依頼を行う。

| No | 画面 | 内容 | 対応方針 |
|---|---|---|---|
| 1 | S4 ランニング設定 | 要件定義書では「ドラムロール・+/-ボタン・直接入力」の3方式ハイブリッド入力としているが、現状の画面は+/-ボタンのみに見える | **要修正**：ドラムロール（スワイプ操作）・直接入力（タップして数値入力）を追加実装する |
| 2 | S3 デバイス接続 | ARグラス（必須）とApple Watch・AirPods（任意）が、画面上は同列の「接続」ボタンで区別がつかない | **要修正**：必須／任意が視覚的に区別できるようにする（ラベル表示・色分け等） |

---

## 5. 未確認・要確認事項

- **S1' 身体情報入力画面**：要件定義書に記載のあるオンボーディング項目だが、スクリーンショットでは未確認。実装状況を確認する
- **権限拒否時のフォールバック画面**（カメラ権限拒否時のHUDモード表示、GPS/BT拒否時のOS設定誘導）：スクリーンショット未確認。実装状況を確認する
- **S7 ラン結果画面の表示項目**：「消費カロリー」表示、「TARGET ACHIEVED」バッジなど、要件定義書に明記していない項目がある。企画書にあった「4段階ランク（Perfect〜Try Again）」表現ではなく、シンクロ率（%）＋達成バッジという表現になっている。要件定義書側への追記要否を検討する
- **S9 設定メニュー系統（S9, S9-1, S9-2, S9-3）**：今回のスクリーンショットには含まれておらず、実装状況が未確認。特にS9-3（ARグラス表示設定）は、HUD表示項目のON/OFFという比較的幅広い設定内容を想定しているため、実装との整合を優先的に確認する
- **S5 前回記録カードの空状態表示**：データがない場合の「-」表示について、スクリーンショットでは未確認。実装状況を確認する

---

## 6. 改訂履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| v1 | 2026-09-04 | 初版作成。実装済み画面（オンボーディング〜履歴表示まで）をもとに画面構成・遷移・要修正事項を整理 |
| v1（更新） | 2026-09-04 | 設定メニュー系統（S9, S9-1〜S9-3）を画面構成・遷移フローに追加。実装状況は未確認のため要確認事項とした |
| v1（更新2） | 2026-09-04 | S5をホーム画面（前回記録表示・ルート提案）に更新。前回記録カードのタップ導線（→S8）を追加 |
