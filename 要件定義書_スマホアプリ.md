- バージョン：v1
- 作成日：2026-09-04
- 開発方式：ウォーターフォール
- ステータス：初版

---

## 1. 文書概要

### 1.1 目的・位置づけ

本文書は、ARペースセッター「AR Vision」のうち、**スマホアプリ側**のUI・コネクティビティ・データ管理に関わる要件を定義するものである。

### 1.2 対象範囲

- 本文書が扱うのは、スマホアプリのUI、機器接続、データ管理に関する要件のみとする。
- ARに関わる判定ロジック（座標計算・危険検知トリガーなど）は本文書のスコープ外とし、将来的に基本設計書側で扱う。
- ARグラス側の表示・体験に関する要件は、別文書「ARグラス側 要件定義書」で管理する。

### 1.3 前提条件

- スマホアプリ側はウォーターフォール開発で進行するため、本文書の内容は開発着手前に確定させ、以降は大幅な変更を行わない前提とする。

---

## 2. 背景・目的

ランナーが一人でペース走を行う際の課題（視線移動によるフォームの乱れ、数値確認による認知負荷、精神的負荷）はARグラス側要件定義書と共通する。

スマホアプリは、走行前の準備（身体情報・目標ペース設定）、走行中の計測、走行後の記録確認・分析結果の閲覧までを一貫して支える役割を担う。

---

## 3. 対象ユーザー・利用シーン

### 3.1 対象ユーザー

- 大会出場を目指すアマチュアランナー（自己ベスト更新が目標）

### 3.2 利用シーン

走行前の準備（身体情報入力・目標ペース設定・機器接続確認）、走行中の計測、走行後の記録確認・分析結果の閲覧、という一連の流れで利用する。

---

## 4. 機能要件

### 4.1 走行分析・データマネジメント

- **リザルト・スコアリング**：走行結果から4段階ランク（Perfect〜Try Again）を判定し、コメントを生成する
- **身体負荷・疲労推定**：気温を係数として、猛暑時は負荷計算を自動補正する
- **セーフティ・ロギング**：急停止・速度超過・逸脱地点をルートマップ上にログとして記録する
- **走行履歴の保存・閲覧**：走行データをアプリ内に保存し、過去の履歴を振り返れるようにする（外部サービスとの連携は行わない）

### 4.2 スマホアプリUI・コネクティビティ

以下、実際の画面遷移順に記載する。

- **オンボーディング**：アプリの概要を紹介する（初回起動時のみ）
- **身体情報入力**：身長・体重・性別を初期入力する（初回起動時のみ）
- **位置情報許可の訴求**：走行計測に必要な位置情報の許可を、地図を用いて分かりやすくユーザーに訴求する（初回起動時のみ）
  - オンボーディング・身体情報入力・位置情報許可の訴求は、初回起動時のみ表示する。2回目以降の起動時は、これら3画面をすべてスキップし、ホーム画面から開始する
  - オンボーディングの見返し・身体情報の再設定は、設定メニューからいつでも行える（後述）
- **ホーム画面（前回記録表示）**：起動時（2回目以降）または初回設定完了後に表示される、アプリのメイン画面。マップ画面上部に、前回の走行記録をカードで表示する。地図は背景として表示する
  - 初回起動などデータがない場合は「-」で情報がないことを示す
  - カードをタップすると、ランニング履歴画面（4.1参照）へ遷移する
  - ハンバーガーメニューから設定メニュー（後述）にアクセスできる
  - ラン開始操作により、以下のデバイス接続・ランニング設定に進む
- **Readyチェック機能（デバイス接続）**：スマホ・グラス（必須）、Watch・イヤホン（任意）の接続状況をインジケーターで管理する
  - 必須／任意は、画面上で視覚的に区別できるようにする（例：ラベル表示、色分けなど）
  - ホーム画面からラン開始操作をするたびに表示する
- **ランニング設定（ハイブリッド入力）**：前回の記録をベースに、以下3方式で目標を設定できる
  - ドラムロール：スワイプで直感的に数値を変更
  - +/-ボタン：最小単位での微調整
  - 直接入力：数値部分をタップし、キーボードから任意の数値を直接入力
- **ルート提案**：現在地周辺のおすすめルートを地図上に表示し、ユーザーが走行前に確認・選択できるようにする（ランニング設定の後、走行開始前に表示する。画面ID：S6）
- **設定メニュー**：ホーム画面のメニューから、以下の項目にアクセスできる
  - オンボーディングの見返し
  - 身体情報の再設定
  - ARグラス表示設定：HUD表示項目（ペース・心拍数など）のON/OFF、走行分析結果を表示するかどうかの設定を含む
  - ランニング履歴の確認（4.1参照）
- **誤操作防止・スリープ制御**：走行開始ボタン押下後、自動スリープを無効化しつつ、誤操作を防ぐガードレイヤーを表示する（画面ID：S7）
- **走行終了後の画面遷移**：ランニング結果画面（S8）で「終了」を選択すると、ホーム画面（S3）に戻る
- **権限管理・フォールバック**：
  - カメラ権限拒否時：アバター表示を停止し、HUD（数値）モードのみで走行計測を継続する
  - 必須権限（GPS/BT）拒否時：出走不可とし、OS設定画面への誘導を表示する

---

## 5. 非機能要件

- **データ保存の信頼性**：走行データがアプリ内に取りこぼしなく保存されること
- **権限フォールバックの分かりやすさ**：権限が拒否された場合も、原因と対処方法がユーザーに伝わる形で案内されること
- **誤操作防止**：走行中に誤ってボタン操作をしてしまうことが起きにくいこと
- **スリープ抑制の安定性**：走行中、意図せず画面がスリープしないこと

---

## 6. 制約条件

- 対象OS：iPhone（iOS）を前提とする ※ウォーターフォールのため、開発期間中の大幅な変更は想定しない
- 周辺機器：Apple Watch・イヤホンは任意接続とし、非接続でも走行計測自体は継続できること
- 走行データの外部サービス（Apple純正ヘルスケアアプリ等）との連携は行わない
- ARの判定ロジック（座標計算・危険検知など）は本文書のスコープ外とし、将来的に基本設計書側で扱う
- 予算・プロジェクト体制などの管理情報は企画書側を参照する

---

## 7. 用語定義

| 用語 | 説明 |
|---|---|
| シンクロ率 / 追従度合い | 設定ペースとの一致度合い。算出はスマホアプリ側 |
| Readyチェック | 走行開始前に、スマホ・グラス・Watch・イヤホンの接続状況を確認する機能 |
| ガードレイヤー | 走行中の誤操作を防ぐための画面上の保護レイヤー |

---

## 8. 改訂履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| v1 | 2026-09-04 | 初版作成。対象ユーザー・利用シーン、機能要件、非機能要件、制約条件を確定 |
| v1（更新） | 2026-09-04 | 実装済み画面のレビューにより、位置情報許可の訴求・ルート提案機能を追記。デバイス接続の必須/任意区別を明記 |
| v1（更新2） | 2026-09-04 | 設定メニュー（オンボーディング見返し・身体情報再設定・ARグラス表示設定・履歴確認）を追記 |
| v1（更新3） | 2026-09-04 | ホーム画面（前回記録表示）を追記。マップ画面上部に前回記録カードを表示し、データがない場合は「-」表示、タップで履歴へ遷移する仕様を明記 |
| v1（更新4） | 2026-09-04 | オンボーディング・身体情報入力は初回起動時のみ表示し、2回目以降はスキップする仕様を明記 |
| v1（更新5） | 2026-09-04 | 実装フロー図（社内共有分）との照合により、画面順序を大幅修正。位置情報許可も初回のみに変更。デバイス接続・ランニング設定はホーム画面の後（ラン開始操作時）に表示される順序に修正。ルート提案はランニング設定の後・ランニング中ロックの前と明記。走行終了後はホーム画面に戻る仕様を追記 |
| v1（更新6） | 2026-09-04 | 画面遷移図に合わせて画面IDを明記（S6=ルート設定、S7=ロック、S8=結果） |
